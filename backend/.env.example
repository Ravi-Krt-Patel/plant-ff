BACKEND_HOST=127.0.0.1
BACKEND_PORT=4000
BACKEND_MODE=demo
BACKEND_STORAGE=memory
BACKEND_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
BACKEND_SECURE_COOKIES=false
# Optional, server-only: set a random string of at least 32 characters to enable demo operations endpoints.
# BACKEND_OPERATIONS_TOKEN=
